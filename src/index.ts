import { startGame  } from '@space-invaders/game';

/**
 * this file does one thing and one thing only: get loaded in the browser, and import the
 * necessarily javascript to start the game
 */
(() => {
	startGame();
})();
